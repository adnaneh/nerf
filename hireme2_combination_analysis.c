// Instrumented version to analyze total_combinations in winning vs losing paths
// This will help understand branching patterns that lead to success

#include <stdint.h>
#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <time.h>
#include <assert.h>
#include <sys/time.h>

typedef uint8_t  u8;
typedef uint32_t u32;

// Copy the confusion matrix and other constants from hireme2.c
static const u8 confusion[512] = {
    ,
    // S_high
    
};

static const u32 diffusion[32] = {
    0xf26cb481,0x16a5dc92,0x3c5ba924,0x79b65248,0x2fc64b18,0x615acd29,0xc3b59a42,0x976b2584,
    0x6cf281b4,0xa51692dc,0x5b3c24a9,0xb6794852,0xc62f184b,0x5a6129cd,0xb5c3429a,0x6b978425,
    0xb481f26c,0xdc9216a5,0xa9243c5b,0x524879b6,0x4b182fc6,0xcd29615a,0x9a42c3b5,0x2584976b,
    0x81b46cf2,0x92dca516,0x24a95b3c,0x4852b679,0x184bc62f,0x29cd5a61,0x429ab5c3,0x84256b97
};

// Data collection variables
static FILE* combo_log = NULL;
static int current_run = 0;

// Simplified versions of needed functions
static double get_time_ms(void) {
    struct timeval tv;
    gettimeofday(&tv, NULL);
    return tv.tv_sec * 1000.0 + tv.tv_usec / 1000.0;
}

static inline u8 dot_row(u32 row, const u8 v[32]) {
    u8 acc = 0;
    while (row) {
        int k = __builtin_ctz(row);
        acc ^= v[k];
        row &= row - 1;
    }
    return acc;
}

static int invert32(const u32 A[32], u32 Ainv[32]) {
    uint64_t aug[32];
    for (int r = 0; r < 32; ++r)
        aug[r] = ((uint64_t)A[r]) | (1ULL << (32 + r));

    for (int c = 0; c < 32; ++c) {
        int piv = c;
        while (piv < 32 && !(aug[piv] >> c & 1)) ++piv;
        if (piv == 32) return -1;
        if (piv != c) { uint64_t tmp = aug[c]; aug[c] = aug[piv]; aug[piv] = tmp; }
        for (int r = 0; r < 32; ++r)
            if (r != c && (aug[r] >> c & 1)) aug[r] ^= aug[c];
    }

    for (int r = 0; r < 32; ++r)
        Ainv[r] = (u32)(aug[r] >> 32);
    return 0;
}

// Global data structures
static u8  inv_low[256][256];
static u8  inv_low_count[256];
static u32 invM[32];

static void precompute(void) {
    // Build S_low⁻¹
    for (int b = 0; b < 256; ++b) inv_low_count[b] = 0;
    for (int x = 0; x < 256; ++x) {
        u8 y = confusion[x];
        inv_low[y][inv_low_count[y]++] = (u8)x;
    }
    
    // Build M⁻¹
    if (invert32(diffusion, invM)) {
        fprintf(stderr, "[FATAL] diffusion matrix not invertible!\n");
        exit(EXIT_FAILURE);
    }
}

// Initialize logging
static void init_logging(void) {
    if (!combo_log) {
        combo_log = fopen("combination_analysis.csv", "w");
        fprintf(combo_log, "run,round,total_combinations,success\n");
    }
}

// Log combination data
static void log_combinations(int round, long long total_combinations, int success) {
    if (combo_log) {
        fprintf(combo_log, "%d,%d,%lld,%d\n", current_run, round, total_combinations, success);
        fflush(combo_log); // Ensure data is written immediately
    }
}

// Instrumented DFS function that logs combination counts
static int dfs_recursive_instrumented(u8 state[32], int round, u8 solution[32]) {
    if (round == 256) {
        memcpy(solution, state, 32);
        return 1;
    }
    
    u8 v[32];
    int valid_state = 1;
    for (int j = 0; j < 32; ++j) {
        v[j] = dot_row(invM[j], state);
        if (inv_low_count[v[j]] == 0) {
            valid_state = 0;
            break;
        }
    }
    
    if (!valid_state) {
        // Log failed path with 0 combinations
        log_combinations(round, 0, 0);
        return 0;
    }
    
    // Calculate total combinations
    int choices_per_pos[32];
    long long total_combinations = 1;
    
    for (int j = 0; j < 32; ++j) {
        choices_per_pos[j] = inv_low_count[v[j]];
        total_combinations *= choices_per_pos[j];
        if (total_combinations > 1000000) {
            // Log early exit
            log_combinations(round, total_combinations, 0);
            return 0;
        }
    }
    
    // Try a few combinations (not all, for performance)
    int max_tries = (total_combinations > 100) ? 100 : (int)total_combinations;
    u8 new_state[32];
    
    for (int try_idx = 0; try_idx < max_tries; ++try_idx) {
        int combo = rand() % (int)total_combinations;
        int temp_combo = combo;
        
        for (int j = 0; j < 32; ++j) {
            int choice_idx = temp_combo % choices_per_pos[j];
            temp_combo /= choices_per_pos[j];
            new_state[j] = inv_low[v[j]][choice_idx];
        }
        
        int result = dfs_recursive_instrumented(new_state, round + 1, solution);
        
        if (result) {
            // Log successful path
            log_combinations(round, total_combinations, 1);
            return 1;
        }
    }
    
    // Log failed path
    log_combinations(round, total_combinations, 0);
    return 0;
}

// Simplified final state builder (just for testing)
static void build_simple_final_state(u8 c[32]) {
    // Create a simple final state for testing
    for (int i = 0; i < 32; i++) {
        c[i] = rand() % 256;
    }
    
    // Make sure it has valid pre-images
    u8 v[32];
    int attempts = 0;
    while (attempts < 10000) {
        int all_valid = 1;
        for (int j = 0; j < 32; ++j) {
            v[j] = dot_row(invM[j], c);
            if (inv_low_count[v[j]] == 0) {
                all_valid = 0;
                break;
            }
        }
        
        if (all_valid) return;
        
        // Modify random position
        c[rand() % 32] = rand() % 256;
        attempts++;
    }
}

int main(void) {
    printf("=== Combination Analysis for Winning vs Losing Paths ===\n");
    
    srand(time(NULL));
    precompute();
    init_logging();
    
    const int NUM_RUNS = 50;
    int successful_runs = 0;
    
    for (current_run = 1; current_run <= NUM_RUNS; current_run++) {
        printf("Run %d/%d: ", current_run, NUM_RUNS);
        
        u8 final_state[32];
        build_simple_final_state(final_state);
        
        u8 solution[32];
        int found = dfs_recursive_instrumented(final_state, 0, solution);
        
        if (found) {
            successful_runs++;
            printf("SUCCESS\n");
        } else {
            printf("FAILED\n");
        }
    }
    
    printf("\nCompleted %d runs. Success rate: %d/%d (%.1f%%)\n", 
           NUM_RUNS, successful_runs, NUM_RUNS, 100.0 * successful_runs / NUM_RUNS);
    
    if (combo_log) {
        fclose(combo_log);
        printf("Combination data saved to combination_analysis.csv\n");
    }
    
    printf("\nAnalysis instructions:\n");
    printf("1. Load combination_analysis.csv into a data analysis tool\n");
    printf("2. Compare total_combinations for success=1 vs success=0 rows\n");
    printf("3. Look for patterns by round number\n");
    printf("4. Identify the 'sweet spot' combination counts that lead to success\n");
    
    return 0;
}